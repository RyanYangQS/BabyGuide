# 养娃不易小程序 - 项目初始化流程

## 一、创建uni-app项目

### 1.1 在HBuilderX中创建项目

1. 打开HBuilderX
2. 点击菜单: `文件` -> `新建` -> `项目`
3. 选择项目类型:
   - 选择 `uni-app`
   - 选择 `Vue 3` + `TypeScript`
   - 模板选择 `默认模板`
4. 填写项目信息:
   - 项目名称: `baby-guide`
   - 项目路径: 选择您的工作目录
5. 点击 `创建`

### 1.2 项目创建后的初始结构

```
baby-guide/
├── pages/              # 页面目录
│   └── index/         # 首页
│       ├── index.vue
│       ├── index.scss
│       └── index.json
├── static/            # 静态资源
├── App.vue            # 应用入口
├── main.ts            # 主入口
├── manifest.json      # 应用配置
├── pages.json         # 页面配置
├── uni.scss           # 全局样式变量
└── tsconfig.json      # TypeScript配置
```

---

## 二、首次运行项目

### 2.1 在HBuilderX中创建项目

1. 在HBuilderX中打开项目
2. 点击菜单: `运行` -> `运行到小程序模拟器` -> `微信开发者工具`
3. 等待编译完成(首次编译需要一些时间)
4. 编译成功后会自动生成 `unpackage/dist/dev/mp-weixin` 目录
5. 微信开发者工具会自动打开

### 2.2 微信开发者工具配置（重要）

当微信开发者工具打开后，需要进行以下配置：

#### AppID 选择

在项目创建界面：

| 选项 | 说明 |
|------|------|
| **测试号** | 推荐，点击"测试号"链接自动获取，可体验大部分功能 |
| **游客 AppID** | 功能受限，无法使用云开发 |
| **正式 AppID** | 需要先在微信公众平台注册小程序 |

**注意**: 如果选择"微信云开发"作为后端服务，必须使用测试号或正式 AppID，游客 AppID 无法使用云开发功能。

#### 后端服务选择

- 如果暂时不需要云开发，选择 **"不使用云服务"**
- 后续可以在微信开发者工具中手动开通云开发

### 2.3 导入项目目录（重要）

**正确的导入目录**：
```
/Users/yangqingsong/workspace/work/self-project/BabyGuide/baby-guide/unpackage/dist/dev/mp-weixin
```

**错误示例**：
```
❌ /Users/yangqingsong/workspace/work/self-project/BabyGuide/baby-guide
   （这是 uni-app 项目根目录，不包含 app.json）
```

**常见错误**：
- 错误信息：`项目根目录未找到 app.json`
- 原因：导入了 uni-app 项目根目录，而不是编译后的微信小程序目录
- 解决：重新导入 `unpackage/dist/dev/mp-weixin` 目录

## 三、配置微信云开发

### 3.1 开通云开发

1. 在微信开发者工具中(已自动打开项目)
2. 点击工具栏的 `云开发` 按钮
3. 开通云开发:
   - 环境名称: `baby-guide-prod`
   - 环境ID: 会自动生成,如 `baby-guide-xxx`
   - 点击 `确定`
4. 记录环境ID,后面会用到

### 3.2 配置云开发环境

在微信开发者工具中:

1. 点击 `云开发` -> `设置` -> `环境设置`
2. 记录 `环境ID`
3. 点击 `数据库` -> 创建集合:
   - `users` (用户表)
   - `children` (儿童档案)
   - `temperature_records` (体温记录)
   - `medicine_records` (用药记录)
   - `symptom_records` (症状记录)
   - `medicines` (药品信息)
   - `family_members` (家庭成员)

---

## 三、安装依赖

### 3.1 在项目根目录打开终端

```bash
# 进入项目目录
cd baby-guide

# 安装基础依赖
npm install

# 安装Pinia状态管理
npm install pinia

# 安装日期处理库
npm install dayjs

# 安装工具库
npm install lodash-es
npm install @types/lodash-es -D
```

---

## 四、创建项目文件结构

### 4.1 创建目录结构

在项目根目录创建以下目录:

```
baby-guide/
├── cloudfunctions/        # 云函数目录
│   ├── login/            # 登录
│   ├── children/         # 儿童档案
│   ├── temperature/      # 体温记录
│   ├── medicine/         # 用药管理
│   ├── symptom/          # 症状记录
│   └── reminder/         # 定时提醒
├── miniprogram/          # 小程序代码(编译后)
├── src/                  # 源代码
│   ├── pages/           # 页面
│   ├── components/      # 组件
│   ├── api/             # API封装
│   ├── store/           # 状态管理
│   ├── utils/           # 工具函数
│   ├── constants/       # 常量
│   └── types/           # TypeScript类型
└── static/              # 静态资源
    ├── images/          # 图片
    └── icons/           # 图标
```

### 4.2 创建命令

在终端执行:

```bash
# 创建云函数目录
mkdir -p cloudfunctions/{login,children,temperature,medicine,symptom,reminder}

# 创建源代码目录
mkdir -p src/{pages,components,api,store,utils,constants,types}

# 创建静态资源目录
mkdir -p static/{images,icons}
```

---

## 五、配置文件修改

### 5.1 修改 manifest.json

打开 `manifest.json`,修改以下内容:

```json
{
  "name": "养娃不易",
  "appid": "__UNI__XXXXXXX",
  "description": "养娃不易小程序",
  "versionName": "1.0.0",
  "versionCode": "100",
  "transformPx": false,
  "mp-weixin": {
    "appid": "your-wechat-appid",
    "setting": {
      "urlCheck": false,
      "es6": true,
      "postcss": true,
      "minified": true
    },
    "usingComponents": true,
    "cloudfunctionRoot": "cloudfunctions/",
    "permission": {
      "scope.userLocation": {
        "desc": "用于获取您的位置信息"
      }
    },
    "requiredPrivateInfos": [
      "chooseImage",
      "chooseVideo"
    ]
  }
}
```

**注意**: 将 `your-wechat-appid` 替换为您的小程序AppID

### 5.2 修改 pages.json

打开 `pages.json`,修改为:

```json
{
  "pages": [
    {
      "path": "pages/index/index",
      "style": {
        "navigationBarTitleText": "首页",
        "navigationBarBackgroundColor": "#FFFFFF",
        "navigationBarTextStyle": "black"
      }
    },
    {
      "path": "pages/temperature/index",
      "style": {
        "navigationBarTitleText": "体温记录"
      }
    },
    {
      "path": "pages/temperature/add",
      "style": {
        "navigationBarTitleText": "添加体温"
      }
    },
    {
      "path": "pages/medicine/index",
      "style": {
        "navigationBarTitleText": "用药管理"
      }
    },
    {
      "path": "pages/medicine/add",
      "style": {
        "navigationBarTitleText": "添加用药"
      }
    },
    {
      "path": "pages/profile/index",
      "style": {
        "navigationBarTitleText": "我的"
      }
    },
    {
      "path": "pages/profile/addChild",
      "style": {
        "navigationBarTitleText": "添加儿童"
      }
    },
    {
      "path": "pages/profile/childDetail",
      "style": {
        "navigationBarTitleText": "儿童详情"
      }
    },
    {
      "path": "pages/login/index",
      "style": {
        "navigationBarTitleText": "登录"
      }
    }
  ],
  "tabBar": {
    "color": "#999999",
    "selectedColor": "#4A90E2",
    "backgroundColor": "#FFFFFF",
    "borderStyle": "white",
    "list": [
      {
        "pagePath": "pages/index/index",
        "text": "首页",
        "iconPath": "static/icons/home.png",
        "selectedIconPath": "static/icons/home-active.png"
      },
      {
        "pagePath": "pages/temperature/index",
        "text": "体温",
        "iconPath": "static/icons/temperature.png",
        "selectedIconPath": "static/icons/temperature-active.png"
      },
      {
        "pagePath": "pages/medicine/index",
        "text": "用药",
        "iconPath": "static/icons/medicine.png",
        "selectedIconPath": "static/icons/medicine-active.png"
      },
      {
        "pagePath": "pages/profile/index",
        "text": "我的",
        "iconPath": "static/icons/profile.png",
        "selectedIconPath": "static/icons/profile-active.png"
      }
    ]
  },
  "globalStyle": {
    "navigationBarTextStyle": "black",
    "navigationBarTitleText": "养娃不易",
    "navigationBarBackgroundColor": "#FFFFFF",
    "backgroundColor": "#F8F9FA"
  }
}
```

### 5.3 创建 project.config.json

在项目根目录创建 `project.config.json`:

```json
{
  "miniprogramRoot": "dist/dev/mp-weixin/",
  "cloudfunctionRoot": "cloudfunctions/",
  "setting": {
    "urlCheck": false,
    "es6": true,
    "enhance": true,
    "postcss": true,
    "preloadBackgroundData": false,
    "minified": true,
    "newFeature": true,
    "coverView": true,
    "nodeModules": false,
    "autoAudits": false,
    "showShadowRootInWxmlPanel": true,
    "scopeDataCheck": false,
    "uglifyFileName": false,
    "checkInvalidKey": true,
    "checkSiteMap": true,
    "uploadWithSourceMap": true,
    "compileHotReLoad": false,
    "lazyloadPlaceholderEnable": false,
    "useMultiFrameRuntime": true,
    "useApiHook": true,
    "useApiHostProcess": true,
    "useIsolateContext": true,
    "userConfirmedBundleSwitch": false,
    "packNpmManually": false,
    "packNpmRelationList": [],
    "minifyWXSS": true,
    "disableUseStrict": false,
    "showES6CompileOption": false,
    "useCompilerPlugins": [
      "typescript"
    ]
  },
  "appid": "your-wechat-appid",
  "projectname": "baby-guide",
  "description": "养娃不易小程序",
  "cloudfunctionTemplateRoot": "",
  "condition": {},
  "editorSetting": {
    "tabIndent": "insertSpaces",
    "tabSize": 2
  }
}
```

**注意**: 将 `your-wechat-appid` 替换为您的小程序AppID

---

## 六、创建云函数

### 6.1 创建登录云函数

在 `cloudfunctions/login/` 目录下创建:

**package.json**
```json
{
  "name": "login",
  "version": "1.0.0",
  "description": "登录云函数",
  "main": "index.js",
  "dependencies": {
    "wx-server-sdk": "~2.6.3"
  }
}
```

**index.js**
```javascript
const cloud = require('wx-server-sdk')

cloud.init({
  env: cloud.DYNAMIC_CURRENT_ENV
})

const db = cloud.database()

exports.main = async (event, context) => {
  const wxContext = cloud.getWXContext()
  
  try {
    // 查询用户是否存在
    const userResult = await db.collection('users')
      .where({
        openid: wxContext.OPENID
      })
      .get()
    
    let user = null
    
    if (userResult.data.length === 0) {
      // 新用户,创建记录
      const result = await db.collection('users').add({
        data: {
          openid: wxContext.OPENID,
          unionid: wxContext.UNIONID || '',
          nickName: event.nickName || '',
          avatarUrl: event.avatarUrl || '',
          phone: '',
          createTime: db.serverDate(),
          updateTime: db.serverDate()
        }
      })
      
      user = {
        _id: result._id,
        openid: wxContext.OPENID,
        isNewUser: true
      }
    } else {
      // 老用户,更新信息
      user = userResult.data[0]
      await db.collection('users').doc(user._id).update({
        data: {
          updateTime: db.serverDate()
        }
      })
    }
    
    return {
      success: true,
      data: user
    }
  } catch (err) {
    console.error(err)
    return {
      success: false,
      errMsg: err.message
    }
  }
}
```

### 6.2 上传云函数

1. 在微信开发者工具中
2. 右键 `cloudfunctions/login` 目录
3. 选择 `上传并部署: 云端安装依赖`
4. 等待上传完成

---

## 七、创建前端代码

### 7.1 创建工具类

**src/utils/cloud.ts**
```typescript
// 云函数调用封装
export async function callFunction(name: string, data?: any) {
  try {
    const result = await wx.cloud.callFunction({
      name,
      data
    })
    
    return result.result as any
  } catch (err) {
    console.error('云函数调用失败:', err)
    throw err
  }
}
```

**src/utils/date.ts**
```typescript
import dayjs from 'dayjs'

// 格式化日期
export function formatDate(date: Date | string, format: string = 'YYYY-MM-DD HH:mm'): string {
  return dayjs(date).format(format)
}

// 计算年龄
export function calculateAge(birthday: string): { years: number; months: number } {
  const birth = dayjs(birthday)
  const now = dayjs()
  
  let years = now.year() - birth.year()
  let months = now.month() - birth.month()
  
  if (months < 0) {
    years--
    months += 12
  }
  
  return { years, months }
}
```

### 7.2 创建API封装

**src/api/index.ts**
```typescript
import { callFunction } from '@/utils/cloud'

// 登录
export function login(data?: any) {
  return callFunction('login', data)
}

// 儿童档案相关
export * from './children'

// 体温记录相关
export * from './temperature'

// 用药管理相关
export * from './medicine'
```

**src/api/children.ts**
```typescript
import { callFunction } from '@/utils/cloud'

// 添加儿童
export function addChild(data: any) {
  return callFunction('children', { action: 'add', data })
}

// 获取儿童列表
export function getChildren() {
  return callFunction('children', { action: 'list' })
}

// 更新儿童
export function updateChild(data: any) {
  return callFunction('children', { action: 'update', data })
}

// 删除儿童
export function deleteChild(childId: string) {
  return callFunction('children', { action: 'delete', data: { childId } })
}
```

### 7.3 创建状态管理

**src/store/index.ts**
```typescript
import { createPinna } from 'pinia'

const pinia = createPinna()

export default pinia
```

**src/store/modules/user.ts**
```typescript
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    userInfo: null as any,
    openid: '',
    isLoggedIn: false
  }),
  
  actions: {
    setUserInfo(userInfo: any) {
      this.userInfo = userInfo
      this.openid = userInfo.openid
      this.isLoggedIn = true
    },
    
    clearUser() {
      this.userInfo = null
      this.openid = ''
      this.isLoggedIn = false
    }
  },
  
  persist: {
    enabled: true,
    strategies: [
      {
        key: 'user',
        storage: {
          getItem: (key: string) => wx.getStorageSync(key),
          setItem: (key: string, value: any) => wx.setStorageSync(key, value)
        }
      }
    ]
  }
})
```

### 7.4 创建页面

**src/pages/login/index.vue**
```vue
<template>
  <view class="login-page">
    <view class="login-container">
      <view class="logo">👶</view>
      <view class="title">养娃不易</view>
      <view class="desc">为宝爸宝妈提供专业的养娃不易服务</view>
      
      <button 
        class="login-btn" 
        open-type="getUserInfo"
        @getuserinfo="handleLogin"
      >
        微信登录
      </button>
    </view>
  </view>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { login } from '@/api'
import { useUserStore } from '@/store/modules/user'

const userStore = useUserStore()
const loading = ref(false)

async function handleLogin(e: any) {
  if (loading.value) return
  
  loading.value = true
  
  try {
    const userInfo = e.detail.userInfo
    const result = await login(userInfo)
    
    if (result.success) {
      userStore.setUserInfo(result.data)
      
      uni.switchTab({
        url: '/pages/index/index'
      })
    } else {
      uni.showToast({
        title: '登录失败',
        icon: 'error'
      })
    }
  } catch (err) {
    console.error(err)
    uni.showToast({
      title: '登录失败',
      icon: 'error'
    })
  } finally {
    loading.value = false
  }
}
</script>

<style lang="scss" scoped>
.login-page {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, #4A90E2 0%, #5BA3F5 100%);
}

.login-container {
  text-align: center;
  padding: 80rpx;
}

.logo {
  font-size: 120rpx;
  margin-bottom: 40rpx;
}

.title {
  font-size: 48rpx;
  font-weight: bold;
  color: white;
  margin-bottom: 24rpx;
}

.desc {
  font-size: 28rpx;
  color: rgba(255, 255, 255, 0.8);
  margin-bottom: 80rpx;
}

.login-btn {
  width: 100%;
  height: 88rpx;
  line-height: 88rpx;
  background: white;
  color: #4A90E2;
  border-radius: 44rpx;
  font-size: 32rpx;
  font-weight: bold;
}
</style>
```

---

## 八、初始化云数据库

### 8.1 添加药品信息

在微信开发者工具中:

1. 点击 `云开发` -> `数据库`
2. 选择 `medicines` 集合
3. 点击 `添加记录`
4. 添加以下数据:

```json
{
  "name": "美林",
  "genericName": "布洛芬",
  "minIntervalHours": 4,
  "maxDailyCount": 4,
  "minAgeMonths": 6,
  "description": "布洛芬混悬液,用于儿童普通感冒或流感引起的发热",
  "createTime": "2026-02-25T00:00:00.000Z"
}
```

```json
{
  "name": "泰诺林",
  "genericName": "对乙酰氨基酚",
  "minIntervalHours": 4,
  "maxDailyCount": 4,
  "minAgeMonths": 3,
  "description": "对乙酰氨基酚混悬液,用于儿童普通感冒或流感引起的发热",
  "createTime": "2026-02-25T00:00:00.000Z"
}
```

---

## 九、运行项目

### 9.1 在HBuilderX中运行

1. 点击菜单: `运行` -> `运行到小程序模拟器` -> `微信开发者工具`
2. 等待编译完成
3. 微信开发者工具会自动打开

### 9.2 在微信开发者工具中调试

1. 查看控制台日志
2. 测试云函数
3. 调试页面功能

---

## 十、下一步

项目初始化完成后,您可以:

1. **开发页面功能**
   - 首页
   - 体温记录
   - 用药管理
   - 我的

2. **开发云函数**
   - 儿童档案管理
   - 体温记录
   - 用药管理
   - 定时提醒

3. **完善功能**
   - 添加图表组件
   - 实现智能提醒
   - 优化用户体验

---

## 十一、常见问题

### Q1: 云函数调用失败?
**A**: 检查云开发环境ID是否配置正确

### Q2: 登录失败?
**A**: 确保已在微信开发者工具中开通云开发

### Q3: 数据库操作失败?
**A**: 检查数据库权限配置

### Q4: 图标不显示?
**A**: 确保图标文件已放入 `static/icons/` 目录

---

## 十二、项目文件清单

初始化完成后,您的项目应该包含以下文件:

```
baby-guide/
├── cloudfunctions/
│   └── login/
│       ├── index.js
│       └── package.json
├── src/
│   ├── api/
│   │   ├── index.ts
│   │   └── children.ts
│   ├── store/
│   │   ├── index.ts
│   │   └── modules/
│   │       └── user.ts
│   ├── utils/
│   │   ├── cloud.ts
│   │   └── date.ts
│   ├── pages/
│   │   ├── index/
│   │   │   ├── index.vue
│   │   │   ├── index.scss
│   │   │   └── index.json
│   │   └── login/
│   │       ├── index.vue
│   │       ├── index.scss
│   │       └── index.json
│   ├── App.vue
│   └── main.ts
├── static/
│   └── icons/
│       ├── home.png
│       ├── home-active.png
│       ├── temperature.png
│       ├── temperature-active.png
│       ├── medicine.png
│       ├── medicine-active.png
│       ├── profile.png
│       └── profile-active.png
├── manifest.json
├── pages.json
├── project.config.json
├── package.json
└── tsconfig.json
```

---

按照这个流程,您就可以完成项目的初始化! 🎊
